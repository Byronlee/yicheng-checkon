- if resource[:tasks]
  - resource[:tasks].each do |task| 
    tr
      td 
        - if resource[:type]=="finished"
          font color="green" = task[:dept_name]
        - else
          font color="red" = task[:dept_name]
      td
        = FormatDate task[:created_at]
      td
        - if resource[:type]=="finished"
          span.alert.alert-success  
            i.icon-ok &nbsp;
            span 已登记
        - else
            form(action="staff_records/update" method="post")
              input name="dept_id" value="#{task[:dept_id]}" type="hidden"
              input name="time" value="#{task[:created_at]}" type="hidden"
              input type="hidden" name="register_way" value="fast_register" 
              .input-append
                = raw(behave_selects nil,{},{:class => "span7",name: "behave_id"})
                button.btn.btn-primary type="type" 保存   
      td
          a href="/staff_records/edit?dept_id=#{task[:dept_id]}&time=#{task[:created_at]}&dept_name=#{task[:dept_name]}" = register_or_modify resource[:type]
          i class="icon-fast-forward"  
