.title
  .content
    strong 伊诚公司2013年3月考勤统计表
table class="table table-condensed"
  tr
    th 工号
    th 姓名
    - Behave.all.map do | bh|
      th = bh.name   
  - if @stats.any?
    - @stats.map do |stat|
      tr 
        td = stat[:user_no]
        td = stat[:username]
        - stat[:behaves].map do |k ,v|
          td = v
